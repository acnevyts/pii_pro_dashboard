import { JsonPathHighlightPipe } from './json-path-highlight.pipe';

describe('JsonPathHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonPathHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
